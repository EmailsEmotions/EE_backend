spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: users-service
  level:
    org.springframework: WARN
    org.hibernate: WARN
  logstash:
      server: localhost
      port: 5000
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config-server:8888
    logstash:
      server: docker
      port: 5000
